// ============================================================
// HealthScore.gs — 健康度判定・改善提案
// ============================================================
//
// 実装は Main.gs 末尾の次の関数にあります（必ず同じプロジェクトに含めてください）。
//   - computeHealthTier_(row)
//   - buildSuggestions_(tier, reasons)
//
// 以前はこのファイルだけに書いていましたが、Apps Script で .gs を1つ
// コピーし忘めると ReferenceError になるため、送信経路で使う定義を Main.gs に集約しました。
//
// このファイルを Apps Script に置かなくても動作します。
// 置く場合はコメントのみでも可ですが、パーサ対策でダミー宣言を1つ置いています。
var HEALTH_SCORE_LOGIC_IN_MAIN_GS = true;
